import TeamDashboardLayout from '../components/ops/TeamDashboardLayout';

/** Platform manager ops — assigned agents or open pool via /api/manager/*. */
export default function TeamDashboardPage() {
  return <TeamDashboardLayout />;
}
