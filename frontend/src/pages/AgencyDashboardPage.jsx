import AgencyDashboardLayout from '../components/ops/AgencyDashboardLayout';

/** Agency tenant ops — scoped to one agency via /api/agency/*. */
export default function AgencyDashboardPage() {
  return <AgencyDashboardLayout />;
}
